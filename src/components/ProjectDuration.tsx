import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const ProjectDuration = (props: Props) => {
  return (
    <View>
      <Text>ProjectDuration</Text>
    </View>
  );
};

export default ProjectDuration;

const styles = StyleSheet.create({});
